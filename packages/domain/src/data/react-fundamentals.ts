import type { Question } from '../session/types'

export const reactFundamentalsQuestions: Question[] = [
  // ── CONCEITOS FUNDAMENTAIS ───────────────────────────────────────────────────
  {
    id: 'rf-001',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'O que é o React e qual problema ele resolve?',
    hints: ['Biblioteca UI', 'Componentes', 'Declarativo'],
    explanation: 'React é uma biblioteca JavaScript declarativa para construir interfaces de usuário, desenvolvida pelo Facebook. Resolve o problema de atualizar o DOM de forma eficiente quando os dados mudam. Em vez de manipular o DOM diretamente (imperativo), você descreve como a UI deve parecer para um dado estado (declarativo), e o React atualiza o DOM automaticamente. Principais características: componentes reutilizáveis, Virtual DOM para performance, fluxo de dados unidirecional.',
    tags: ['fundamentos', 'react', 'o-que-e', 'declarativo'],
  },
  {
    id: 'rf-002',
    domain: 'react',
    type: 'multiple_choice',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'Quais são as principais características do React?',
    options: [
      { id: 'a', text: 'Componentes, Virtual DOM, fluxo de dados unidirecional e JSX', isCorrect: true },
      { id: 'b', text: 'Componentes, banco de dados embutido, two-way binding e templates HTML', isCorrect: false },
      { id: 'c', text: 'MVC, Virtual DOM, framework completo e two-way binding', isCorrect: false },
      { id: 'd', text: 'Componentes, jQuery integrado, CSS-in-JS e servidor embutido', isCorrect: false },
    ],
    hints: ['React é uma biblioteca, não um framework completo'],
    explanation: 'As principais características do React: (1) Componentes: peças de UI reutilizáveis e composáveis; (2) Virtual DOM: representação em memória do DOM para atualizações eficientes; (3) Fluxo de dados unidirecional: dados fluem de pai para filho via props; (4) JSX: sintaxe que mescla JavaScript e HTML; (5) Declarativo: descreva O QUE renderizar, não COMO manipular o DOM.',
    tags: ['fundamentos', 'caracteristicas', 'componentes', 'virtual-dom'],
  },
  {
    id: 'rf-003',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'O que é JSX e por que ele é usado no React?',
    hints: ['Extensão de sintaxe JavaScript', 'Parece HTML mas é JS', 'Transpilado por Babel'],
    explanation: 'JSX (JavaScript XML) é uma extensão de sintaxe do JavaScript que permite escrever código parecido com HTML dentro de arquivos JS. É transpilado por Babel para chamadas `React.createElement()`. Ex: `<Button color="blue">Click</Button>` vira `React.createElement(Button, {color:"blue"}, "Click")`. Vantagens: mais legível, verificação de tipos em tempo de compilação com TypeScript, facilita a visualização da estrutura de componentes. JSX não é HTML — usa `className`, `htmlFor`, e eventos camelCase (`onClick`).',
    tags: ['jsx', 'fundamentos', 'babel', 'sintaxe'],
  },
  {
    id: 'rf-004',
    domain: 'react',
    type: 'multiple_choice',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'Qual é a diferença entre um Elemento React e um Componente React?',
    options: [
      { id: 'a', text: 'Elemento é um objeto simples descrevendo o que renderizar; Componente é uma função/classe que retorna elementos', isCorrect: true },
      { id: 'b', text: 'Elemento é um componente de classe; Componente é uma função', isCorrect: false },
      { id: 'c', text: 'Não há diferença, os termos são sinônimos', isCorrect: false },
      { id: 'd', text: 'Elemento é qualquer tag HTML; Componente é apenas elementos customizados', isCorrect: false },
    ],
    hints: ['`React.createElement()` retorna um Elemento (objeto plain)', 'Componente aceita props e retorna elementos'],
    explanation: 'Elemento React: objeto JavaScript simples descrevendo o que deve aparecer na tela. Ex: `{ type: "div", props: { children: "Olá" } }`. É imutável. Componente React: função ou classe que aceita props e retorna elementos. Componentes são "fábricas" de elementos. Ao escrever `<MinhaApp/>`, você está criando um elemento cujo `type` é o componente `MinhaApp`.',
    tags: ['elemento', 'componente', 'diferenca', 'fundamentos'],
  },
  {
    id: 'rf-005',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'Quais são as formas de criar componentes no React? Explique cada uma.',
    hints: ['Componente funcional', 'Componente de classe', 'Hooks tornaram os funcionais mais poderosos'],
    explanation: 'Existem duas formas: (1) Componente Funcional: função que recebe props e retorna JSX. `function Btn({label}) { return <button>{label}</button>; }`. Moderna, mais simples, suporta Hooks. (2) Componente de Classe: extends `React.Component`, tem método `render()`, ciclo de vida explícito. `class Btn extends React.Component { render() { return <button>{this.props.label}</button>; } }`. Hoje componentes funcionais são preferidos em todos os casos. Classes são legadas mas ainda suportadas.',
    tags: ['componentes', 'funcional', 'classe', 'hooks', 'fundamentos'],
  },
  {
    id: 'rf-006',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Quando você ainda usaria um componente de classe em vez de um funcional?',
    hints: ['Error Boundaries', 'getSnapshotBeforeUpdate', 'Projeto legado'],
    explanation: 'Em 2024+, quase nunca. A única razão técnica para usar componente de classe hoje é implementar Error Boundaries, pois `componentDidCatch` e `getDerivedStateFromError` ainda não têm equivalentes em Hooks. Para projetos legados que já usam classes, pode fazer sentido manter consistência. Em qualquer novo código, use componentes funcionais com Hooks — são mais simples, testáveis e composáveis.',
    tags: ['classe', 'funcional', 'error-boundary', 'legado', 'decisao'],
  },
  {
    id: 'rf-007',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são Pure Components no React? Qual o equivalente em componentes funcionais?',
    hints: ['shouldComponentUpdate', 'React.memo', 'Shallow comparison'],
    explanation: '`React.PureComponent` é uma classe base que implementa `shouldComponentUpdate` com comparação superficial (shallow comparison) de props e state — evita re-renders desnecessários quando os valores não mudaram. O equivalente funcional é `React.memo()`, que envolve um componente e só re-renderiza se as props mudarem (também por shallow comparison). Cuidado: objetos/arrays criados inline sempre serão "novos" na shallow comparison, anulando a otimização.',
    tags: ['pure-component', 'memo', 'performance', 'shallow-comparison', 'otimizacao'],
  },
  {
    id: 'rf-008',
    domain: 'react',
    type: 'multiple_choice',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'O que é o estado (state) em um componente React?',
    options: [
      { id: 'a', text: 'Dados privados do componente que, quando alterados, disparam uma re-renderização', isCorrect: true },
      { id: 'b', text: 'Dados passados de pai para filho que o componente pode modificar', isCorrect: false },
      { id: 'c', text: 'Uma variável global acessível por qualquer componente', isCorrect: false },
      { id: 'd', text: 'A URL atual da página', isCorrect: false },
    ],
    hints: ['State é interno ao componente', 'Muda com `useState` ou `setState`'],
    explanation: 'State é o conjunto de dados internos de um componente que podem mudar ao longo do tempo. Em componentes funcionais, gerenciamos state com `useState`. Quando o state muda via `setState` (classe) ou o setter do `useState` (função), o React re-renderiza o componente com os novos valores. State é privado — apenas o componente que o possui pode modificá-lo diretamente.',
    tags: ['state', 'useState', 're-render', 'fundamentos'],
  },
  {
    id: 'rf-009',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'O que são props no React? Como diferem do state?',
    hints: ['Passadas de pai para filho', 'São imutáveis no filho', 'State é interno'],
    explanation: 'Props (propriedades) são dados passados de um componente pai para um componente filho. São imutáveis no filho — o filho não pode modificá-las diretamente (fluxo unidirecional). Diferenças: (1) Props vêm de fora (pai), state é interno; (2) Props são imutáveis no filho, state é mutável via setter; (3) Props são para configuração/comunicação pai→filho, state é para dados que mudam com o tempo dentro do componente.',
    tags: ['props', 'state', 'diferenca', 'fundamentos', 'unidirecional'],
  },
  {
    id: 'rf-010',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Por que não devemos atualizar o state diretamente? O que acontece se fizermos isso?',
    hints: ['React não detecta a mutação', 'Não dispara re-render', 'setState é assíncrono'],
    explanation: 'Atualizar state diretamente (`this.state.count = 5` ou `state.items.push(x)`) não dispara re-renderização — o React não detecta a mutação. O componente não atualiza na tela. Além disso, a mutação direta quebra imutabilidade, dificultando debugging e otimizações como `PureComponent`/`React.memo`. Sempre use o setter: `setCount(5)` ou `setState({ count: 5 })`. Para arrays/objetos, crie um novo: `setItems([...items, novoItem])`.',
    tags: ['state', 'imutabilidade', 're-render', 'anti-pattern', 'useState'],
  },
  {
    id: 'rf-011',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual a finalidade da função de callback em `setState()` de componentes de classe?',
    hints: ['setState é assíncrono', 'Callback é executado após re-render', 'Acessa o novo state'],
    explanation: '`setState()` em componentes de classe é assíncrono — o state não é atualizado imediatamente. Se você precisar executar código após a atualização ser aplicada e o componente re-renderizado, passe um callback: `this.setState({ count: 1 }, () => { console.log(this.state.count) // 1 })`. Sem o callback, `this.state.count` logo após o `setState` ainda seria o valor antigo. Em componentes funcionais, use `useEffect` para reagir a mudanças de state.',
    tags: ['setState', 'callback', 'assincrono', 'classe', 'ciclo-de-vida'],
  },
  {
    id: 'rf-012',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual a diferença entre o tratamento de eventos no HTML e no React?',
    hints: ['camelCase vs lowercase', 'Funções vs strings', 'Eventos sintéticos'],
    explanation: 'Diferenças: (1) Nome: HTML usa lowercase (`onclick`), React usa camelCase (`onClick`); (2) Handler: HTML aceita string (`"handleClick()"`), React aceita função (`{handleClick}`); (3) Prevenir padrão: HTML retorna `false`, React chama `e.preventDefault()`; (4) Eventos React são Synthetic Events — wrappers cross-browser normalizados; (5) React usa delegação de eventos — todos os handlers ficam no root, não no elemento individual.',
    tags: ['eventos', 'html', 'react', 'camelCase', 'synthetic-events'],
  },
  {
    id: 'rf-013',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como passar um parâmetro para um manipulador de eventos em React?',
    hints: ['Arrow function inline', 'bind no JSX', 'Evite bind no render por performance'],
    explanation: 'Formas: (1) Arrow function inline: `<button onClick={() => handleClick(id)}>`. Cria nova função a cada render — pode impactar performance em listas grandes. (2) Método de classe com bind no constructor: `this.handleClick = this.handleClick.bind(this, id)`. (3) Arrow function como property: `handleClick = (id) => ...` com bind implícito. A abordagem mais comum em funcionais é a arrow function inline. Para otimização em listas, use `useCallback` com o parâmetro embutido.',
    tags: ['eventos', 'parametro', 'arrow-function', 'bind', 'handlers'],
  },
  {
    id: 'rf-014',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são Synthetic Events (eventos sintéticos) no React?',
    hints: ['Wrapper cross-browser', 'Interface consistente', 'Event pooling (legado)'],
    explanation: 'Synthetic Events são wrappers cross-browser criados pelo React que normalizam a API de eventos do navegador. Você obtém a mesma interface de evento independentemente do navegador (Chrome, Firefox, Safari). Implementam a mesma interface do `Event` nativo. Historicamente, o React usava "event pooling" — reutilizava objetos de evento por performance, e acessar o evento de forma assíncrona resultava em valores null. No React 17+, o pooling foi removido, então você pode acessar propriedades de eventos de forma assíncrona sem problemas.',
    tags: ['synthetic-events', 'cross-browser', 'event-pooling', 'react17'],
  },
  {
    id: 'rf-015',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são expressões condicionais inline no React? Quais as formas disponíveis?',
    hints: ['if ternário', 'Operador &&', 'if-else com variável'],
    explanation: 'Renderização condicional inline em JSX: (1) Operador ternário: `{isLoggedIn ? <UserPanel/> : <Login/>}` — melhor para dois casos; (2) Operador &&: `{isLoaded && <Content/>}` — renderiza o segundo operando se o primeiro for truthy. CUIDADO: `{count && <div/>}` renderiza `0` quando count é 0 — use `{count > 0 && <div/>}`; (3) Variável: defina o JSX em uma variável `let el = null; if(x) el = <A/>; return el`; (4) IIFE: `{(() => { if(x) return <A/>; return <B/>; })()}`.',
    tags: ['condicional', 'ternario', 'operador-and', 'renderizacao', 'jsx'],
  },
  {
    id: 'rf-016',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'O que é a prop `key` em React? Por que deve ser única nas listas?',
    hints: ['Ajuda a reconciliação', 'Identificar qual item mudou', 'Não use index como key quando possível'],
    explanation: 'A prop `key` é usada pelo React durante a reconciliação para identificar quais itens de uma lista mudaram, foram adicionados ou removidos. Deve ser única entre irmãos (não globalmente). Por que não usar index: se você reordenar, adicionar ou remover itens do meio da lista, o React associará a key errada ao elemento errado, causando bugs sutis de estado e performance. Use IDs estáveis do dado (ex: `item.id`). Usar índice é aceitável apenas para listas estáticas sem reordenação.',
    tags: ['key', 'listas', 'reconciliacao', 'index', 'performance'],
  },
  {
    id: 'rf-017',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são refs no React? Qual a diferença entre `useRef` e `createRef`?',
    hints: ['Acesso direto ao DOM', 'useRef persiste entre renders', 'createRef cria nova ref a cada render'],
    explanation: 'Refs permitem acessar diretamente nós do DOM ou valores que persistem entre renders sem causar re-renders. `createRef()`: cria uma nova ref a cada chamada — usado em componentes de classe (no constructor). `useRef()`: Hook que retorna sempre o mesmo objeto `{ current }` entre renders — ideal para componentes funcionais. Ambos são atribuídos via `ref={myRef}` no JSX. Use casos: focar um input, integrar com bibliotecas não-React, animações imperativas, armazenar timers.',
    tags: ['refs', 'useRef', 'createRef', 'dom', 'diferenca'],
  },
  {
    id: 'rf-018',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que são forward refs? Quando e como usar `React.forwardRef`?',
    hints: ['Passar ref de pai para filho', 'Componentes de biblioteca', 'useImperativeHandle'],
    explanation: 'Por padrão, refs não são passadas via props — a prop `ref` é especial. `React.forwardRef` permite que um componente "encaminhe" uma ref recebida de seu pai para um elemento DOM interno. Exemplo: `const FancyInput = React.forwardRef((props, ref) => <input ref={ref} {...props}/>)`. O pai pode então focar o input diretamente. Usado em design systems e bibliotecas de componentes. Combine com `useImperativeHandle` para expor apenas uma API controlada ao invés do DOM inteiro.',
    tags: ['forwardRef', 'refs', 'dom', 'design-system', 'useImperativeHandle'],
  },
  {
    id: 'rf-019',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Por que String Refs são consideradas legadas no React?',
    hints: ['`ref="myInput"`', 'Problemas de performance e composição', 'Substituídas por callback refs e useRef'],
    explanation: 'String refs (`ref="myInput"`) eram a forma original de criar refs, acessadas via `this.refs.myInput`. Problemas: (1) Requerem que o React rastreie o componente em execução — performance ruim; (2) Não funcionam bem com HOCs e composição; (3) Não são verificadas pelo compilador; (4) Não suportam lazy evaluation. Foram removidas no React 19. Prefira `useRef` (funcionais) ou callback refs (`ref={(el) => this.input = el}`) que são mais flexíveis.',
    tags: ['string-refs', 'legado', 'refs', 'deprecated'],
  },
  {
    id: 'rf-020',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que é "Lifting State Up" (elevação de estado) no React?',
    hints: ['Compartilhar estado entre irmãos', 'Mover state para o pai comum', 'Props down, callbacks up'],
    explanation: '"Lifting State Up" é o padrão de mover o state para o ancestral comum mais próximo quando dois ou mais componentes precisam compartilhar e sincronizar dados. O pai mantém o state e passa tanto o valor (via props) quanto a função de atualização (via callback props) para os filhos. Exemplo: dois inputs de temperatura (Celsius e Fahrenheit) devem ser sincronizados — o estado da temperatura fica no pai que os contém. Fluxo: dados descem (props), eventos sobem (callbacks).',
    tags: ['lifting-state', 'compartilhar-estado', 'pai', 'callbacks', 'props'],
  },
  // ── DOM, FIBER E PROCESSAMENTO ────────────────────────────────────────────────
  {
    id: 'rf-021',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que é o React Fiber? Qual o seu objetivo principal?',
    hints: ['Reescrita do algoritmo de reconciliação', 'Renderização incremental', 'Prioridade de atualizações'],
    explanation: 'React Fiber é a reescrita do algoritmo de reconciliação do React (introduzida no React 16). Objetivo principal: tornar o rendering incremental e interrompível. No modelo antigo (Stack Reconciler), a reconciliação era síncrona e não podia ser pausada — animações e interações do usuário podiam ser bloqueadas. Com Fiber: (1) O trabalho é dividido em unidades pequenas ("fibras"); (2) O React pode pausar, retomar e priorizar trabalho; (3) Habilita Concurrent Mode, Suspense, `useTransition`, `useDeferredValue`.',
    tags: ['fiber', 'reconciliacao', 'concurrent-mode', 'incremental', 'performance'],
  },
  {
    id: 'rf-022',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Qual a diferença entre Shadow DOM e Virtual DOM?',
    hints: ['Shadow DOM: encapsulamento CSS/DOM nativo', 'Virtual DOM: representação em memória para diffing'],
    explanation: 'São conceitos distintos sem relação direta. Virtual DOM: representação em memória do DOM real usada pelo React para calcular atualizações eficientes (diffing). É puramente JavaScript. Shadow DOM: tecnologia de browser nativa para encapsulamento de CSS e DOM em Web Components — o CSS interno não vaza para fora, e o CSS externo não entra. React não usa Shadow DOM (a menos que você explicitamente integre Web Components). Vue.js também usa Virtual DOM; Shadow DOM é uma API de browser padrão.',
    tags: ['shadow-dom', 'virtual-dom', 'diferenca', 'web-components', 'encapsulamento'],
  },
  {
    id: 'rf-023',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são componentes controlados vs não controlados?',
    hints: ['Controlado: React controla o valor', 'Não controlado: DOM controla o valor', 'ref para não controlados'],
    explanation: 'Componente Controlado: o valor do input é controlado pelo state React. `<input value={state} onChange={e => setState(e.target.value)}>`. React é a "single source of truth". Permite validação em tempo real, transformação de input, desabilitação condicional. Componente Não Controlado: o DOM mantém o valor. Você acessa via `ref` quando necessário (`inputRef.current.value`). Mais simples para formulários simples, mas dificulta validação em tempo real. Preferência geral: controlados para mais controle; não controlados para integração com código não-React.',
    tags: ['controlado', 'nao-controlado', 'formularios', 'refs', 'state'],
  },
  {
    id: 'rf-024',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual a diferença entre `React.createElement` e `React.cloneElement`?',
    hints: ['createElement: cria novo elemento', 'cloneElement: clona e mescla props'],
    explanation: '`React.createElement(type, props, children)`: cria um novo elemento React do zero. É o que o JSX compila. `React.cloneElement(element, newProps, newChildren)`: clona um elemento existente e mescla novas props — mantém as props originais e adiciona/sobrescreve com as novas. Caso de uso: em HOCs ou padrões como Render Props, o pai clona o filho injetando props extras. Exemplo: `React.cloneElement(child, { onClick: handleClick })`.',
    tags: ['createElement', 'cloneElement', 'diferenca', 'hoc', 'props'],
  },
  {
    id: 'rf-025',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Quais são as fases do ciclo de vida de um componente React?',
    hints: ['Montagem', 'Atualização', 'Desmontagem'],
    explanation: 'Três fases principais: (1) Montagem (Mounting): componente criado e inserido no DOM. Métodos de classe: `constructor → getDerivedStateFromProps → render → componentDidMount`. Hooks: `useState` (init) → render → `useEffect` (após mount); (2) Atualização (Updating): props ou state mudam. Métodos: `getDerivedStateFromProps → shouldComponentUpdate → render → getSnapshotBeforeUpdate → componentDidUpdate`. Hooks: render → `useEffect` (com deps); (3) Desmontagem (Unmounting): componente removido do DOM. `componentWillUnmount`. Hooks: cleanup do `useEffect`.',
    tags: ['ciclo-de-vida', 'lifecycle', 'montagem', 'desmontagem', 'atualizacao'],
  },
  {
    id: 'rf-026',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que são Higher-Order Components (HOCs)? Quando usar vs Hooks?',
    hints: ['Função que recebe componente e retorna componente', 'Cross-cutting concerns', 'Hoje: prefira Hooks'],
    explanation: 'HOC é uma função que recebe um componente e retorna um novo componente aprimorado. Padrão de composição para reutilização de lógica. Ex: `withAuth(Component)` retorna componente que verifica autenticação antes de renderizar. Problemas: props collision, wrapper hell, dificulta rastreamento no DevTools. Com Hooks (desde React 16.8), a maioria dos casos de HOC pode ser substituída por custom hooks — mais simples e sem os problemas. Use HOCs hoje apenas quando precisar modificar o que é renderizado (não apenas lógica).',
    tags: ['hoc', 'higher-order-component', 'hooks', 'composicao', 'reutilizacao'],
  },
  {
    id: 'rf-027',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que é o contexto (Context API) no React? Quando deve e quando NÃO deve ser usado?',
    hints: ['Evitar prop drilling', 'Todos consumidores re-renderizam ao mudar', 'Não substitui Zustand/Redux'],
    explanation: 'Context API permite compartilhar dados entre componentes sem passar props manualmente por cada nível. Criado com `createContext`, provido por `<Provider value={...}>`, consumido com `useContext`. Quando usar: dados globais estáticos ou de baixa frequência de mudança — tema, idioma, usuário autenticado. Quando NÃO usar: dados que mudam com frequência (ex: estado de formulário, contador em tempo real) — todos os consumidores do Context re-renderizam a cada mudança do `value`. Para isso, use Zustand, Jotai ou Redux.',
    tags: ['context', 'prop-drilling', 'performance', 're-render', 'zustand'],
  },
  {
    id: 'rf-028',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'O que é a prop `children` no React?',
    hints: ['Conteúdo entre tags de abertura e fechamento', 'Composição de componentes'],
    explanation: '`children` é uma prop especial que representa o conteúdo passado entre as tags de abertura e fechamento de um componente. `<Card><p>Conteúdo</p></Card>` — dentro do Card, `props.children` é o `<p>`. Permite composição flexível — o Card não precisa saber antecipadamente o que será renderizado dentro dele. O tipo de `children` pode ser qualquer coisa: string, elemento, array de elementos, ou função (render props pattern). Em TypeScript: `React.ReactNode` para qualquer conteúdo renderizável.',
    tags: ['children', 'composicao', 'props', 'render-props'],
  },
  {
    id: 'rf-029',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que é reconciliação no React? Explique o processo de diffing.',
    hints: ['Comparar Virtual DOM antigo vs novo', 'Algoritmo O(n)', 'Heurísticas: tipo e key'],
    explanation: 'Reconciliação é o processo pelo qual o React determina o que mudou no Virtual DOM e atualiza o DOM real de forma mínima. O algoritmo de diffing do React usa heurísticas para rodar em O(n): (1) Elementos de tipos diferentes criam uma nova árvore (destrói e cria do zero); (2) Elementos do mesmo tipo: atualiza apenas os atributos que mudaram; (3) Listas: usa `key` para identificar quais itens mudaram, foram adicionados ou removidos. Sem `key`, React tem de reordenar pela posição — ineficiente para listas dinâmicas.',
    tags: ['reconciliacao', 'diffing', 'virtual-dom', 'performance', 'algoritmo'],
  },
  {
    id: 'rf-030',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como definir state com um nome de chave dinâmico em React?',
    hints: ['Computed property names do ES6', '[variavel]: valor'],
    explanation: 'Use computed property names do ES6 para definir chaves dinâmicas: `setState({ [fieldName]: value })` em classes, ou `setForm(prev => ({ ...prev, [fieldName]: value }))` com hooks. Exemplo prático em formulários genéricos: `const handleChange = (e) => { const { name, value } = e.target; setForm(prev => ({ ...prev, [name]: value })); }`. Isso permite um único handler para todos os inputs do formulário, usando o atributo `name` do input como chave.',
    tags: ['state', 'chave-dinamica', 'computed-property', 'formulario', 'es6'],
  },
  // ── CICLO DE VIDA E HOOKS ────────────────────────────────────────────────────
  {
    id: 'rf-031',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Por que usar `super(props)` no constructor de componentes de classe? E super() sem props?',
    hints: ['Inicializar React.Component', 'this.props no constructor', 'Sempre passe props ao super'],
    explanation: 'Em classes que herdam (`extends React.Component`), você DEVE chamar `super()` antes de acessar `this`. Passar `props` para `super(props)` permite que `this.props` seja acessado dentro do constructor. Se chamar apenas `super()`, `this.props` será `undefined` DENTRO do constructor (mas funcionará nos métodos de ciclo de vida, pois o React atribui props separadamente). Por segurança e clareza, sempre chame `super(props)`. Em React moderno com campos de classe, o constructor é raramente necessário.',
    tags: ['constructor', 'super', 'props', 'classe', 'heranca'],
  },
  {
    id: 'rf-032',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Por que React usa `className` em vez do atributo `class` do HTML?',
    hints: ['class é palavra reservada em JavaScript', 'JSX é JavaScript'],
    explanation: 'Como JSX é transpilado para JavaScript, e `class` é uma palavra reservada em JS (para definir classes), o React optou por `className` para evitar conflito. O mesmo vale para `htmlFor` (em vez do atributo `for` do HTML, que também é palavra reservada em JS — usado em loops). O babel/compilador converte `className` para o atributo `class` no HTML gerado. Em React 18+, você pode usar `class` no JSX em alguns casos via otimizações do compilador, mas `className` é o padrão.',
    tags: ['className', 'class', 'jsx', 'palavra-reservada', 'html'],
  },
  {
    id: 'rf-033',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'O que são Fragments no React? Por que são melhores que wrapper divs?',
    hints: ['Retornar múltiplos elementos sem nó extra', '<></>', 'Sem impacto no DOM'],
    explanation: 'Fragments permitem retornar múltiplos elementos sem adicionar um nó extra ao DOM. Sintaxe: `<React.Fragment>...</React.Fragment>` ou shorthand `<>...</>`. Por que melhor que `<div>`: (1) Não polui o DOM com divs desnecessárias; (2) Evita problemas de layout CSS (flexbox, grid podem ser afetados por divs extras); (3) Melhora performance — menos nós no DOM; (4) Semântica correta (ex: `<tr>` só pode ter `<td>` como filho direto, não `<div>`). Use `<React.Fragment key={id}>` quando precisar de key em listas.',
    tags: ['fragments', 'dom', 'performance', 'semantica', 'jsx'],
  },
  {
    id: 'rf-034',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são Portals no React? Quais são os casos de uso?',
    hints: ['Renderizar fora do pai DOM', 'Modais, tooltips, dropdowns', 'Eventos ainda bubblam normalmente'],
    explanation: '`ReactDOM.createPortal(children, domNode)` renderiza o filho em um nó DOM diferente do pai React, enquanto mantém o contexto React normal (eventos bubblam pela árvore React, não pela árvore DOM). Casos de uso clássicos: modais (evita problemas de `overflow: hidden` do pai), tooltips e dropdowns que precisam escapar do contexto de clipping, notificações. O portal é filho lógico do componente React que o criou, mas filho DOM de outro nó — melhor dos dois mundos.',
    tags: ['portals', 'modal', 'tooltip', 'dom', 'contexto'],
  },
  {
    id: 'rf-035',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são Error Boundaries no React? Como implementar?',
    hints: ['Capturar erros de renderização', 'componentDidCatch', 'Apenas componentes de classe'],
    explanation: 'Error Boundaries são componentes de classe que capturam erros de JavaScript na árvore de componentes abaixo deles, logam e exibem uma UI fallback. Implementação: uma classe com `static getDerivedStateFromError(error)` (retorna state para mostrar fallback) e/ou `componentDidCatch(error, info)` (para logging). Não capturam: erros em event handlers (use try/catch), código assíncrono, erros no próprio Error Boundary. Use-os estrategicamente: um no topo (fallback geral) e um por seção crítica da UI. Não há equivalente em Hooks até React 19.',
    tags: ['error-boundary', 'erros', 'fallback', 'componentDidCatch', 'classe'],
  },
  {
    id: 'rf-036',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que são componentes stateless e stateful?',
    hints: ['Stateless: sem state interno', 'Stateful: gerencia state'],
    explanation: 'Stateless (sem estado): componente que não usa state — apenas recebe props e renderiza. São simples, previsíveis e fáceis de testar. Ex: componente de UI puro como `<Button>`. Stateful (com estado): componente que mantém e gerencia seu próprio state (com `useState` ou `useReducer`). Pode reagir a interações do usuário. A tendência moderna é ter poucos componentes stateful (containers ou páginas) e muitos stateless — facilita reutilização e testes. Com Hooks, qualquer componente funcional pode ser stateful.',
    tags: ['stateless', 'stateful', 'state', 'componentes', 'arquitetura'],
  },
  {
    id: 'rf-037',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como aplicar validação de props no React com PropTypes?',
    hints: ['prop-types package', 'defaultProps', 'TypeScript é preferido hoje'],
    explanation: 'PropTypes (pacote `prop-types`): valida tipos de props em desenvolvimento. Ex: `MyComp.propTypes = { name: PropTypes.string.isRequired, age: PropTypes.number, onClick: PropTypes.func }`. Tipos: `string`, `number`, `bool`, `array`, `object`, `func`, `node`, `element`, `shape({})`, `arrayOf()`. `defaultProps` define valores padrão. Em 2024+, TypeScript é preferido sobre PropTypes — verificação em tempo de compilação, não em runtime. Com TypeScript: `interface Props { name: string; age?: number }` tipado diretamente.',
    tags: ['propTypes', 'validacao', 'typescript', 'defaultProps', 'tipos'],
  },
  {
    id: 'rf-038',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que é o método `getDerivedStateFromProps`? Quando usar?',
    hints: ['Derivar state das props', 'Raramente necessário', 'Alternativas mais simples existem'],
    explanation: '`static getDerivedStateFromProps(props, state)` é chamado antes de cada render (tanto na montagem quanto nas atualizações) e retorna um objeto para mesclar ao state ou `null`. Caso de uso: quando o state precisa ser atualizado com base em mudanças de props (ex: redefinir estado quando uma prop "ID" muda). RARAMENTE necessário. Na maioria dos casos, existem alternativas melhores: key para resetar estado, memorizar computações, ou buscar dados em `componentDidUpdate`. Anti-padrão frequente: copiar props para o state sem necessidade.',
    tags: ['getDerivedStateFromProps', 'lifecycle', 'estado-derivado', 'anti-pattern'],
  },
  {
    id: 'rf-039',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que é `getSnapshotBeforeUpdate`? Qual o caso de uso clássico?',
    hints: ['Capturar informação do DOM antes de atualizar', 'Posição de scroll', 'Retorna valor passado ao componentDidUpdate'],
    explanation: '`getSnapshotBeforeUpdate(prevProps, prevState)` é chamado imediatamente antes do DOM ser atualizado. Permite capturar informação do DOM (como posição de scroll) antes que ela mude. O valor retornado é passado como terceiro argumento ao `componentDidUpdate(prevProps, prevState, snapshot)`. Caso de uso clássico: chat com scroll — ao adicionar novas mensagens no topo, salve a posição de scroll atual no snapshot e restaure em `componentDidUpdate` para manter a posição do usuário.',
    tags: ['getSnapshotBeforeUpdate', 'lifecycle', 'scroll', 'dom', 'snapshot'],
  },
  {
    id: 'rf-040',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Os Hooks substituem render props e HOCs completamente?',
    hints: ['Hooks substituem a maioria dos casos', 'HOC ainda útil para wrap de JSX', 'Render props: Hooks são mais simples'],
    explanation: 'Na prática, sim para a grande maioria dos casos. Hooks substituem com vantagem a lógica reutilizável que antes exigia HOCs e render props, sem o "wrapper hell" e sem problemas de props collision. Porém, HOCs ainda são úteis quando você precisa modificar o que é renderizado (não só adicionar lógica). Ex: `withAuth(Component)` retorna componente diferente — isso é difícil de fazer com Hooks. Render props são quase completamente substituídas por Hooks — ex: `useMousePosition()` em vez de `<MouseTracker render={pos => ...}/>`.',
    tags: ['hooks', 'render-props', 'hoc', 'composicao', 'substituicao'],
  },
  // ── PERFORMANCE E MISCELÂNEA ─────────────────────────────────────────────────
  {
    id: 'rf-041',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Por que passar uma função para `setState()` em vez de um objeto?',
    hints: ['setState é assíncrono', 'Batching', 'Acesso ao state anterior correto'],
    explanation: 'Quando atualizações de state dependem do state anterior, passe uma função: `setState(prev => ({ count: prev.count + 1 }))`. Motivo: `setState` é assíncrono e o React pode fazer batching de múltiplas chamadas. Se você chamar `setState({ count: state.count + 1 })` duas vezes seguidas, ambas podem ler o mesmo `state.count` e o resultado final incrementa apenas 1 vez. Com a forma funcional, cada chamada recebe o state mais atualizado como `prev`. Em `useState`, o setter também aceita função: `setCount(c => c + 1)`.',
    tags: ['setState', 'funcao', 'assincrono', 'batching', 'state-anterior'],
  },
  {
    id: 'rf-042',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que é Strict Mode no React? O que ele detecta?',
    hints: ['Desenvolvimento apenas', 'Detecta side effects em renders', 'Renderiza duas vezes em dev'],
    explanation: '`<React.StrictMode>` é um wrapper que ativa verificações e avisos adicionais em desenvolvimento (não impacta produção). Detecta: (1) Métodos de ciclo de vida legados/inseguros; (2) String refs; (3) findDOMNode legado; (4) Efeitos colaterais inesperados — renderiza componentes duas vezes (render + state initializer) para expor efeitos não-puros; (5) Uso de Context API legada. Em React 18, também destaca o uso de `startTransition` incorreto. Use em toda a app para detectar problemas cedo.',
    tags: ['strict-mode', 'desenvolvimento', 'deteccao', 'efeitos-colaterais', 'qualidade'],
  },
  {
    id: 'rf-043',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Por que `isMounted()` é considerado um anti-padrão? Como evitar o problema?',
    hints: ['Verificar se o componente ainda está montado antes de setState', 'Memory leak', 'Cancelar async operations'],
    explanation: '`isMounted()` era usado para evitar `setState` após unmount (causaria warning). Anti-padrão porque: (1) Encoraja ignorar o problema em vez de resolvê-lo; (2) A causa raiz é um async operation que continua após unmount. Solução correta: cancelar a operação assíncrona no cleanup do `useEffect`. Para fetch: use `AbortController`. Para timers: `clearTimeout`/`clearInterval`. Para subscriptions: cancele no retorno do useEffect. Assim, a operação é cancelada quando o componente desmonta, nunca tentando atualizar state.',
    tags: ['isMounted', 'anti-pattern', 'memory-leak', 'async', 'cleanup', 'useEffect'],
  },
  {
    id: 'rf-044',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual é a diferença entre `constructor` (classe) e `getInitialState` (legado)?',
    hints: ['createReactClass vs ES6 classes', 'Legado da era pre-ES6'],
    explanation: '`getInitialState` era o método usado com a antiga API `React.createClass()` (era pre-ES6). `constructor` é o método padrão das classes ES6 que substituíram `createClass`. Com ES6: `class Comp extends React.Component { constructor(props) { super(props); this.state = {...} } }`. Com createClass (legado): `getInitialState() { return {...} }`. O `createClass` foi separado em um pacote próprio (`create-react-class`) no React 15.5 e é considerado legado. Em código moderno, use classes ES6 ou (melhor ainda) componentes funcionais com `useState`.',
    tags: ['constructor', 'getInitialState', 'legado', 'createReactClass', 'es6'],
  },
  {
    id: 'rf-045',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Pode forçar um re-render sem chamar setState? Como?',
    hints: ['forceUpdate() em classes', 'Não recomendado', 'Indica problema de design'],
    explanation: 'Em componentes de classe: `this.forceUpdate()` força um re-render pulando `shouldComponentUpdate`. Não recomendado — indica que algo externo ao state/props controla a UI, violando o modelo de dados do React. Em componentes funcionais: não há equivalente direto. Workarounds: `const [, forceRender] = useReducer(x => x + 1, 0)` e chamar `forceRender()`. Na maioria dos casos, se você precisa forçar re-render, o design está errado — o dado que deveria disparar o render deveria estar no state.',
    tags: ['forceUpdate', 're-render', 'anti-pattern', 'design', 'state'],
  },
  {
    id: 'rf-046',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual a diferença entre `super()` e `super(props)` em componentes de classe?',
    hints: ['this.props disponível no constructor', 'React atribui props externamente'],
    explanation: 'Em JavaScript, quando uma classe herda de outra, `super()` deve ser chamado antes de acessar `this`. Com `super(props)`: `this.props` está disponível dentro do constructor imediatamente. Com `super()` (sem props): `this.props` é `undefined` dentro do constructor, mas funcionará normalmente nos outros métodos de ciclo de vida (pois o React atribui `this.props` externamente após o constructor). Na prática, sempre passe props: `super(props)` — é mais claro e seguro. A diferença só importa se você acessa `this.props` no constructor.',
    tags: ['super', 'props', 'constructor', 'heranca', 'classe'],
  },
  {
    id: 'rf-047',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'Como fazer um loop dentro do JSX para renderizar uma lista?',
    hints: ['Array.map()', 'Chave key obrigatória', 'JSX aceita arrays de elementos'],
    explanation: 'A forma mais comum é usar `Array.map()` que retorna um array de elementos JSX: `{items.map(item => <li key={item.id}>{item.name}</li>)}`. Alternativas: `Array.from()`, `Array.filter().map()`. Em JSX, você só pode usar expressões (não statements como `for` ou `if`). Sempre inclua a prop `key` ao renderizar listas. Para lógica mais complexa, extraia para uma variável antes do return: `const listItems = items.map(...)` e então `{listItems}` no JSX.',
    tags: ['loop', 'map', 'lista', 'jsx', 'key'],
  },
  {
    id: 'rf-048',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que é `React.PropTypes array with shape`? Como tipar um array de objetos?',
    hints: ['PropTypes.arrayOf(PropTypes.shape({}))', 'TypeScript: Array<Tipo>'],
    explanation: 'Para validar um array de objetos com forma específica usando PropTypes: `PropTypes.arrayOf(PropTypes.shape({ id: PropTypes.number.isRequired, name: PropTypes.string.isRequired, active: PropTypes.bool }))`. Isso valida que a prop é um array onde cada elemento tem a forma especificada. O equivalente TypeScript (preferido): `items: Array<{ id: number; name: string; active?: boolean }>` ou com interface `items: Item[]`. TypeScript oferece verificação em tempo de compilação, enquanto PropTypes é apenas runtime em desenvolvimento.',
    tags: ['propTypes', 'arrayOf', 'shape', 'typescript', 'validacao-tipos'],
  },
  {
    id: 'rf-049',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual a diferença entre `React` e `ReactDOM`?',
    hints: ['React: componentes e lógica', 'ReactDOM: interface com o DOM do browser'],
    explanation: 'Foram separados intencionalmente no React 0.14: `react` contém a lógica central de componentes, hooks, context — agnóstico de plataforma. `react-dom` é a implementação para browsers, lida com o DOM real. Esta separação permite o mesmo React ser usado com `react-native` (iOS/Android), `react-three-fiber` (3D), `react-pdf`, etc. — cada um com seu próprio "renderer". Em desenvolvimento normal, você importa de ambos: componentes e hooks de `react`, e `createRoot`/`render` de `react-dom`.',
    tags: ['react', 'react-dom', 'separacao', 'renderer', 'multiplataforma'],
  },
  {
    id: 'rf-050',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual a diferença entre `setState` e `replaceState` em componentes de classe?',
    hints: ['setState mescla', 'replaceState substitui completamente', 'replaceState foi removido'],
    explanation: '`setState(newState)` faz MERGE do novo estado com o estado existente — apenas as chaves fornecidas são atualizadas. `replaceState(newState)` SUBSTITUÍA completamente o estado — chaves não presentes em `newState` eram removidas. `replaceState` foi removido do React e não está disponível desde a migração para ES6 classes (só existia com `createReactClass`). Em componentes funcionais com `useState`, o setter também substitui completamente a variável de estado (não faz merge) — mas com objetos de state divididos, isso é menos relevante.',
    tags: ['setState', 'replaceState', 'merge', 'substitui', 'legado'],
  },
  // ── PERGUNTAS ADICIONAIS PRÁTICAS ──────────────────────────────────────────
  {
    id: 'rf-051',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que acontece quando você usa `setState` dentro do `constructor` de uma classe?',
    hints: ['Não é necessário', 'Apenas inicialize com `this.state =`', 'Causaria erro em dev'],
    explanation: 'Chamar `setState` dentro do constructor causará um erro ou aviso. No constructor, o state ainda não foi inicializado. A forma correta de inicializar o state é `this.state = { chave: valor }` diretamente. Fora do constructor (como em `componentDidMount`), `setState` é a forma correta de atualizar. O equivalente em hooks: `useState(valorInicial)` onde `valorInicial` é avaliado apenas no primeiro render.',
    tags: ['setState', 'constructor', 'inicializacao', 'classe'],
  },
  {
    id: 'rf-052',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual o impacto de usar índices (index) como `key` em listas React?',
    hints: ['Reordenação causa problemas', 'State associado ao índice, não ao item', 'Focus e input values'],
    explanation: 'Usar index como key causa problemas quando a lista é reordenada, filtrada ou itens são inseridos/removidos no meio. O React associa o state ao index, não ao dado. Exemplo: lista com inputs — ao reordenar, o texto digitado no input "vai para" o item errado porque a key (índice) permanece igual mas agora aponta para outro dado. Use index APENAS para listas verdadeiramente estáticas (sem reordenação, adição ou remoção). Para qualquer lista dinâmica, use um ID único e estável do dado.',
    tags: ['key', 'index', 'anti-pattern', 'lista', 'reordenacao'],
  },
  {
    id: 'rf-053',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como aplicar atributos de classe condicionalmente em React?',
    hints: ['Template literals', 'clsx / classnames library', 'Ternário com className'],
    explanation: 'Formas: (1) Template literal: `className={\`btn ${isActive ? "active" : ""} ${disabled ? "disabled" : ""}\`}`; (2) Biblioteca `clsx` ou `classnames` (preferida para múltiplas condições): `className={clsx("btn", { active: isActive, disabled: disabled, large: size === "lg" })}`; (3) Array com filter: `className={["btn", isActive && "active", disabled && "disabled"].filter(Boolean).join(" ")}`. A biblioteca `clsx` é a abordagem mais legível e popular em projetos React.',
    tags: ['className', 'condicional', 'clsx', 'classnames', 'css'],
  },
  {
    id: 'rf-054',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como combinar múltiplos objetos de estilo inline no React?',
    hints: ['Spread operator', 'Object.assign', 'Limitações de estilos inline'],
    explanation: 'Combine com spread: `style={{ ...baseStyle, ...conditionalStyle, color: "red" }}`. Ou com Object.assign: `style={Object.assign({}, baseStyle, { color: "red" })}`. A última definição de uma propriedade vence. Limitações de estilos inline: não suportam pseudo-classes (`:hover`), media queries, animações complexas. Para esses casos, prefira CSS Modules, Tailwind ou CSS-in-JS (styled-components, Emotion).',
    tags: ['style', 'inline', 'spread', 'objeto', 'css-in-js'],
  },
  {
    id: 'rf-055',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como renderizar a UI quando o navegador é redimensionado?',
    hints: ['window.addEventListener("resize")', 'Custom hook useWindowSize', 'Cleanup do event listener'],
    explanation: 'Implemente um custom hook: `function useWindowSize() { const [size, setSize] = useState({ width: window.innerWidth, height: window.innerHeight }); useEffect(() => { const handler = () => setSize({ width: window.innerWidth, height: window.innerHeight }); window.addEventListener("resize", handler); return () => window.removeEventListener("resize", handler); }, []); return size; }`. Use com debounce para melhor performance — o evento resize dispara muitas vezes por segundo.',
    tags: ['resize', 'window', 'custom-hook', 'useEffect', 'event-listener'],
  },
  {
    id: 'rf-056',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Quais são as vantagens e limitações do React?',
    hints: ['Vantagens: ecossistema, reatividade, componentização', 'Limitações: não é framework completo, curva de aprendizado'],
    explanation: 'Vantagens: (1) Grande ecossistema e comunidade; (2) Componentização e reutilização; (3) Virtual DOM para performance; (4) Flexibilidade — escolha suas próprias soluções para roteamento, state, etc.; (5) React Native para mobile; (6) Meta por trás com investimento contínuo. Limitações: (1) Não é um framework completo — você precisa escolher e integrar muitas libs; (2) Ritmo rápido de mudanças; (3) JSX pode ser barreira inicial; (4) Re-renders excessivos se não otimizado; (5) Sem opinião sobre estrutura — pode resultar em inconsistência em times grandes.',
    tags: ['vantagens', 'limitacoes', 'react', 'tradeoffs', 'ecossistema'],
  },
  {
    id: 'rf-057',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que é o pacote `react-dom` e o método `render`?',
    hints: ['Ponto de entrada no DOM', 'createRoot no React 18', 'Montagem do app'],
    explanation: '`react-dom` é a biblioteca que conecta React ao DOM do browser. No React 18+: `const root = ReactDOM.createRoot(document.getElementById("root")); root.render(<App/>)`. Antes do React 18: `ReactDOM.render(<App/>, document.getElementById("root"))`. O `createRoot` habilitou o Concurrent Mode. `react-dom` também fornece `createPortal`, `flushSync`, `findDOMNode` (legado). Para testes: use `@testing-library/react` que usa `react-dom` internamente.',
    tags: ['react-dom', 'render', 'createRoot', 'react18', 'montagem'],
  },
  {
    id: 'rf-058',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que é ReactDOMServer? Para que é utilizado?',
    hints: ['SSR', 'renderToString', 'renderToPipeableStream'],
    explanation: '`ReactDOMServer` renderiza componentes React para HTML estático — base do Server-Side Rendering (SSR). Métodos: `renderToString(element)`: gera HTML como string, inclui atributos de hidratação; `renderToStaticMarkup(element)`: HTML sem atributos extras (para emails, PDFs). No React 18: `renderToPipeableStream` (Node.js) e `renderToReadableStream` (Web Streams) — streaming SSR, enviam HTML progressivamente conforme disponível. Usado pelo Next.js, Remix e outros frameworks SSR para gerar HTML no servidor antes de enviar ao browser.',
    tags: ['ReactDOMServer', 'ssr', 'renderToString', 'streaming', 'nextjs'],
  },
  {
    id: 'rf-059',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como usar `innerHTML` no React com segurança?',
    hints: ['dangerouslySetInnerHTML', 'XSS', 'Sanitizar o HTML'],
    explanation: 'React usa `dangerouslySetInnerHTML={{ __html: htmlString }}` (o nome é intencional como aviso). Risco: XSS — nunca use com conteúdo fornecido diretamente pelo usuário sem sanitização. Para sanitizar: use a biblioteca `DOMPurify` antes: `dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(userContent) }}`. Casos de uso legítimos: renderizar HTML de um CMS, emails HTML, conteúdo gerado por rich text editors. Prefira substituir por componentes React sempre que possível.',
    tags: ['dangerouslySetInnerHTML', 'xss', 'seguranca', 'dompurify', 'sanitizar'],
  },
  {
    id: 'rf-060',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como implementar Server-Side Rendering (SSR) com React?',
    hints: ['Next.js', 'ReactDOMServer', 'Hidratação no cliente'],
    explanation: 'Processo: (1) Servidor: importa o componente, chama `ReactDOMServer.renderToString(<App/>)` e insere o HTML na página HTML; (2) Browser: recebe HTML pré-renderizado (rápido para o usuário), carrega o bundle React, e `ReactDOM.hydrateRoot` "hidrata" — adiciona event listeners ao HTML existente sem re-renderizar. Hoje, use frameworks: Next.js (SSR com `getServerSideProps` ou App Router), Remix. Configuração manual com Express + Webpack é complexa e raramente necessária.',
    tags: ['ssr', 'server-side-rendering', 'hidracao', 'nextjs', 'renderToString'],
  },
  {
    id: 'rf-061',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como ativar o modo de produção no React? Quais as diferenças?',
    hints: ['NODE_ENV=production', 'Sem PropTypes warnings', 'Bundle menor'],
    explanation: 'Definir `NODE_ENV=production` ativa o modo de produção. Diferenças vs desenvolvimento: (1) Sem verificações de PropTypes (performance); (2) Sem warnings de ciclo de vida legado; (3) Sem Strict Mode double-rendering; (4) Bundle menor — webpack/rollup removem código morto (`if(process.env.NODE_ENV !== "production") {...}`); (5) Sem React DevTools Tools extras. Com Vite/CRA/Next.js, isso é configurado automaticamente nos comandos de build (`npm run build`). Nunca suba código em modo desenvolvimento para produção — é significativamente mais lento.',
    tags: ['producao', 'NODE_ENV', 'build', 'bundle', 'performance'],
  },
  {
    id: 'rf-062',
    domain: 'react',
    type: 'open_text',
    difficulty: 1,
    targetLevel: ['junior'],
    text: 'Qual a ordem correta dos métodos de ciclo de vida na montagem de um componente de classe?',
    hints: ['constructor → render → componentDidMount'],
    explanation: 'Ordem na montagem: (1) `constructor(props)` — inicializa state; (2) `static getDerivedStateFromProps(props, state)` — deriva state das props iniciais; (3) `render()` — retorna JSX; (4) `componentDidMount()` — chamado após inserção no DOM (ótimo para fetch de dados, subscriptions). O DOM está disponível a partir do `componentDidMount`. Equivalente em hooks: `useState` e inicialização → render → `useEffect(() => {}, [])` (com deps vazio = executado após montagem).',
    tags: ['ciclo-de-vida', 'ordem', 'montagem', 'constructor', 'componentDidMount'],
  },
  {
    id: 'rf-063',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Quais métodos de ciclo de vida foram depreciados no React v16.3+?',
    hints: ['UNSAFE_ prefixo', 'componentWillMount', 'componentWillUpdate', 'componentWillReceiveProps'],
    explanation: 'Três métodos foram marcados com prefixo `UNSAFE_` (ainda funcionam mas serão removidos): (1) `UNSAFE_componentWillMount` (anteriormente `componentWillMount`) — rodava antes do render inicial; (2) `UNSAFE_componentWillReceiveProps` (anteriormente `componentWillReceiveProps`) — substituído por `getDerivedStateFromProps`; (3) `UNSAFE_componentWillUpdate` (anteriormente `componentWillUpdate`) — substituído por `getSnapshotBeforeUpdate`. Foram depreciados porque com Concurrent Mode, eles podem ser chamados múltiplas vezes, tornando efeitos colaterais dentro deles problemáticos.',
    tags: ['deprecated', 'UNSAFE', 'ciclo-de-vida', 'concurrent-mode', 'migracao'],
  },
  {
    id: 'rf-064',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Qual a forma recomendada de nomear componentes React?',
    hints: ['PascalCase', 'Nomes descritivos', 'Componentes de arquivo único'],
    explanation: 'Convenções: (1) PascalCase para componentes: `UserProfile`, `PaymentButton` — necessário para que o JSX saiba que é um componente (não uma tag HTML); (2) camelCase para funções utilitárias: `formatDate`, `fetchUser`; (3) Nomes descritivos e específicos — evite `Component`, `Item`, `Card` genéricos; (4) Prefixo com contexto: `AuthModal`, `ProductCard`, `UserAvatar`; (5) Arquivos com o mesmo nome do componente: `UserProfile.tsx`; (6) Hooks com prefixo `use`: `useAuth`, `useDebounce`.',
    tags: ['nomenclatura', 'PascalCase', 'convencao', 'boas-praticas'],
  },
  {
    id: 'rf-065',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'O que é um componente de alternância (Switching Component)? Como implementar?',
    hints: ['Renderizar componente baseado em condição', 'Switch/case ou objeto mapa', 'Evite if-else em cascata'],
    explanation: 'Um componente de alternância renderiza diferentes componentes com base em um valor (similar a switch/case). Implementação com objeto mapa (mais limpa): `const COMPONENTS = { login: LoginForm, signup: SignupForm, forgot: ForgotPassword }; function AuthPage({ view }) { const Component = COMPONENTS[view]; return Component ? <Component/> : null; }`. Vantagens sobre if-else em cascata: mais legível, facilmente extensível (adicione nova entrada no mapa), evita re-renderizações desnecessárias.',
    tags: ['switching-component', 'condicional', 'mapa', 'componentes', 'arquitetura'],
  },
  {
    id: 'rf-066',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como lidar com o atributo `for` nos elementos `<label>` em React?',
    hints: ['htmlFor em vez de for', 'Palavra reservada JavaScript'],
    explanation: 'O atributo HTML `for` em `<label>` é usado para vincular ao `id` de um input. Em JSX, use `htmlFor` (pois `for` é palavra reservada em JavaScript para loops). `<label htmlFor="email">Email:</label> <input id="email" type="email"/>`. Clicar no label foca o input associado — importante para acessibilidade. `htmlFor` é convertido para `for` no HTML renderizado pelo React.',
    tags: ['htmlFor', 'label', 'acessibilidade', 'jsx', 'palavra-reservada'],
  },
  {
    id: 'rf-067',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Quais são os Pointer Events suportados no React?',
    hints: ['Touchscreen e caneta', 'onPointerDown', 'Substitui eventos de mouse separados'],
    explanation: 'React suporta todos os Pointer Events da spec W3C: `onPointerDown`, `onPointerMove`, `onPointerUp`, `onPointerCancel`, `onPointerEnter`, `onPointerLeave`, `onPointerOver`, `onPointerOut`, `onGotPointerCapture`, `onLostPointerCapture`. Pointer Events unificam mouse, touch e stylus/pen em uma única API — você não precisa lidar com `mousedown` E `touchstart` separadamente. Propriedades úteis: `pointerType` ("mouse", "pen", "touch"), `pressure`, `tiltX`, `tiltY`.',
    tags: ['pointer-events', 'touch', 'mouse', 'pen', 'api'],
  },
  {
    id: 'rf-068',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Por que nomes de componentes React devem começar com letra maiúscula?',
    hints: ['JSX compila para createElement', 'String vs referência', 'HTML vs componente React'],
    explanation: 'No JSX, elementos que começam com letra minúscula são tratados como tags HTML nativas (`<div>`, `<span>`). Elementos que começam com maiúscula são tratados como componentes React. `<button>` → `React.createElement("button")` (string). `<Button>` → `React.createElement(Button)` (referência à variável Button). Se você criasse um componente `button` (minúsculo), o React tentaria criar uma tag HTML "button" em vez do seu componente. É uma convenção necessária, não opcional.',
    tags: ['PascalCase', 'nomenclatura', 'jsx', 'createElement', 'convencao'],
  },
  {
    id: 'rf-069',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Atributos DOM customizados são suportados no React?',
    hints: ['React 16+ aceita', 'Antes causava warning', 'data-* e aria-*'],
    explanation: 'A partir do React 16, atributos DOM customizados são passados ao DOM sem warnings — útil para integração com bibliotecas que usam atributos customizados. Antes do 16, apenas atributos HTML/SVG conhecidos eram passados. Atributos `data-*` e `aria-*` sempre foram suportados. Exemplo: `<div data-testid="my-element" custom-attr="value"/>`. Em TypeScript, você pode precisar estender os tipos para evitar erros de tipo em atributos customizados: `interface HTMLAttributes<T> { "custom-attr"?: string }`.',
    tags: ['atributos-customizados', 'dom', 'react16', 'data-attributes', 'aria'],
  },
  {
    id: 'rf-070',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Como acessar props dentro de aspas de atributos no JSX?',
    hints: ['Chaves {} em vez de aspas', 'Interpolação de expressões'],
    explanation: 'Em JSX, você não pode interpolar expressões dentro de strings com aspas. Use chaves `{}` para passar qualquer expressão JavaScript: ERRADO: `<img alt="Foto de {user.name}"/>`. CORRETO: `<img alt={\`Foto de ${user.name}\`}/>` ou `<img alt={"Foto de " + user.name}/>`. Para valores puramente dinâmicos: `<Component value={variavel}/>`. Strings literais podem usar aspas: `<Component type="button"/>`. As chaves `{}` marcam o início de uma expressão JavaScript no JSX.',
    tags: ['jsx', 'aspas', 'chaves', 'expressoes', 'interpolacao'],
  },
  // ── REDUX E ESTADO GLOBAL ─────────────────────────────────────────────────────
  {
    id: 'rf-071',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Redux vs Context API: quando usar cada um?',
    hints: ['Context: dados simples e estáticos', 'Redux: estado complexo, frequente, debuggability'],
    explanation: 'Context API: dados globais simples (tema, idioma, usuário logado), que mudam raramente. Sem ferramentas de debugging sofisticadas. Redux: estado global complexo, com lógica de atualização elaborada, que precisa de DevTools (time-travel debugging, action log), testabilidade de reducers, middleware (logging, analytics). Hoje, Redux Toolkit reduziu muito o boilerplate. Alternativa moderna: Zustand — simplicidade do Context com poder próximo ao Redux. Regra prática: Context para "configuração global", gerenciadores de estado para "dados dinâmicos".',
    tags: ['redux', 'context', 'comparacao', 'estado-global', 'zustand'],
  },
  {
    id: 'rf-072',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['junior', 'pleno'],
    text: 'Por que o state do Redux deve ser imutável?',
    hints: ['Comparação por referência', 'Time-travel debugging', 'Detectar mudanças eficientemente'],
    explanation: 'Imutabilidade é fundamental no Redux por: (1) Detecção eficiente de mudanças: o React-Redux usa comparação por referência (===) — se a referência mudou, re-renderiza; se não, otimiza. Mutação direta mantém a mesma referência, impossibilitando a detecção. (2) Time-travel debugging: imutabilidade permite "voltar no tempo" — cada estado anterior é um snapshot imutável. (3) Previsibilidade: dado o mesmo estado + ação, sempre produz o mesmo novo estado. (4) Pure reducers: facilita testes unitários. Redux Toolkit usa Immer internamente para escrita "mutável" que produz estado imutável.',
    tags: ['redux', 'imutabilidade', 'reducers', 'time-travel', 'performance'],
  },
  {
    id: 'rf-073',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Explique middleware no Redux. Como funciona? Exemplos comuns.',
    hints: ['Entre dispatch e reducer', 'Composta em pipeline', 'Logging, async, analytics'],
    explanation: 'Middleware é código que intercepta entre o `dispatch` de uma action e o reducer processar essa action. Compostos em pipeline: `dispatch → middleware1 → middleware2 → reducer`. API: `store => next => action => { /* lógica */ return next(action) }`. Exemplos: (1) `redux-logger`: loga actions e state antes/depois; (2) `redux-thunk`: permite dispatch de funções (async) — incluído no Redux Toolkit; (3) `redux-saga`: gerencia efeitos colaterais com generators; (4) Middleware de analytics: envia eventos para tracking. Adicionados via `applyMiddleware()` ou `configureStore` do RTK.',
    tags: ['redux', 'middleware', 'thunk', 'saga', 'pipeline'],
  },
  {
    id: 'rf-074',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Qual problema o Redux Toolkit (RTK) resolve? Quais são seus principais APIs?',
    hints: ['Reduz boilerplate', 'createSlice', 'createAsyncThunk', 'Immer embutido'],
    explanation: 'O Redux clássico tinha muito boilerplate: action types, action creators, reducers com switch, setup do store. RTK resolve: (1) `configureStore`: setup simplificado com middleware padrão (thunk, DevTools); (2) `createSlice`: gera action types, action creators e reducer em um só lugar, com Immer para escrita "mutável" mas segura; (3) `createAsyncThunk`: padroniza fluxo de async (pending/fulfilled/rejected); (4) `createSelector` (Reselect): selectors memoizados; (5) `createEntityAdapter`: CRUD normalizado. RTK é hoje a forma oficial e recomendada de usar Redux.',
    tags: ['redux-toolkit', 'rtk', 'createSlice', 'boilerplate', 'immer'],
  },
  {
    id: 'rf-075',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que são selectors no Redux? Por que usar Reselect?',
    hints: ['Derivar dados do state', 'Memoização', 'Evitar recomputação'],
    explanation: 'Selectors são funções que extraem e derivam dados do Redux state: `const getCompletedTodos = (state) => state.todos.filter(t => t.done)`. Problema: se chamada em cada render, recria o array filtrado toda vez, causando re-renders desnecessários. Reselect: cria selectors memoizados — `createSelector(inputSelectors, resultFn)`. Recalcula apenas quando os inputs mudam. `const getCompletedTodos = createSelector([selectTodos], todos => todos.filter(t => t.done))`. RTK exporta `createSelector` diretamente do Reselect.',
    tags: ['selectors', 'reselect', 'memoizacao', 'redux', 'performance'],
  },
  {
    id: 'rf-076',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como funcionam ações assíncronas no Redux? Compare thunk vs saga.',
    hints: ['Thunk: função que retorna função', 'Saga: generators para side effects', 'Testabilidade vs simplicidade'],
    explanation: 'Thunk: action creator retorna uma função `(dispatch, getState) => { await fetch(...); dispatch(success(data)) }`. Simples, fácil de aprender. Redux Toolkit inclui `createAsyncThunk`. Saga: usa generators para descrever fluxo assíncrono de forma síncrona. Vantagens da saga: testabilidade superior (sem mocks de network), cancelamento nativo, race conditions, takeLatest/takeLeading built-in. Desvantagem: curva de aprendizado com generators. Para a maioria dos projetos, thunk+RTK é suficiente. Saga brilha em fluxos complexos com cancelamento e orchestration.',
    tags: ['redux', 'thunk', 'saga', 'async', 'comparacao', 'generators'],
  },
  {
    id: 'rf-077',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como gerenciar estado de erro global em uma aplicação React?',
    hints: ['Context + reducer', 'Redux slice para erros', 'Error Boundary + logging'],
    explanation: 'Abordagens: (1) Redux: slice dedicado `uiSlice` com `{ errors: [], loading: {} }` — actions para adicionar/remover erros; (2) Context: `ErrorContext` com lista de erros e função `addError`/`clearError`; (3) React Query/TanStack Query: erros de server state tratados localmente por query; (4) Error Boundary: captura erros de renderização. Estratégia completa: erros de API → mensagem toast (não bloqueia UI); erros críticos → Error Boundary → tela de fallback; erros de renderização → Sentry para logging.',
    tags: ['erro', 'global', 'redux', 'context', 'error-boundary', 'gestao'],
  },
  {
    id: 'rf-078',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que é estado normalizado (normalized state)? Por que usar?',
    hints: ['Estrutura como banco de dados relacional', 'IDs como referências', 'Evita duplicação'],
    explanation: 'Estado normalizado estrutura dados como um banco relacional: objetos indexados por ID em vez de arrays aninhados. Antes (desnormalizado): `[{ id:1, name:"A", posts:[{id:10, title:"X"}] }]`. Depois (normalizado): `{ users: { ids:[1], entities:{1:{id:1,name:"A"}} }, posts: { ids:[10], entities:{10:{id:10,title:"X",userId:1}} } }`. Vantagens: atualizações O(1) por ID, sem duplicação de dados, sem atualizações aninhadas complexas. RTK tem `createEntityAdapter` para gerenciar estrutura normalizada.',
    tags: ['normalizacao', 'redux', 'estado', 'arquitetura', 'createEntityAdapter'],
  },
  {
    id: 'rf-079',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['pleno'],
    text: 'Explique Zustand como alternativa ao Redux. Quais suas vantagens?',
    hints: ['Sem Provider', 'API simples', 'Sem boilerplate', 'Funciona fora de componentes'],
    explanation: 'Zustand é uma biblioteca de state management minimalista. Criação: `const useStore = create((set) => ({ count: 0, inc: () => set(s => ({ count: s.count+1 })) }))`. Vantagens vs Redux: (1) Sem Provider wrapping; (2) Zero boilerplate; (3) Funciona fora de componentes (acesso via `store.getState()`); (4) Seletores automáticos — componente só re-renderiza quando seu slice muda; (5) Middleware incluído (immer, devtools, persist). Desvantagem: menos estrutura imposta — em times grandes pode gerar inconsistência.',
    tags: ['zustand', 'redux', 'alternativa', 'state-management', 'provider'],
  },
  {
    id: 'rf-080',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Quando a Context API NÃO deve ser usada para gerenciamento de estado?',
    hints: ['Atualiza frequentemente', 'Todos os consumidores re-renderizam', 'Performar ou não com contexto de alta frequência'],
    explanation: 'Evite Context para state que muda frequentemente (ex: posição do mouse, valor de input em tempo real, timer). Motivo: quando `value` do Provider muda, TODOS os consumidores do Context re-renderizam, mesmo que não usem a parte que mudou. Isso pode causar performance ruim. Soluções: (1) Dividir o Context em partes (um para dados que mudam, outro para dados estáticos); (2) Memoização estratégica com `useMemo` e `React.memo`; (3) Usar Zustand ou Jotai que fazem seleção granular — componente só re-renderiza se sua fração do state mudou.',
    tags: ['context', 'performance', 're-render', 'limite', 'zustand', 'jotai'],
  },
  // ── PERFORMANCE AVANÇADA ─────────────────────────────────────────────────────
  {
    id: 'rf-081',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como otimizar a renderização de uma tabela com 10.000 linhas?',
    hints: ['Virtualização', 'react-virtual ou react-window', 'Só renderiza o visível'],
    explanation: 'Nunca renderize 10k linhas no DOM. Use virtualização: a biblioteca renderiza apenas as linhas visíveis na viewport + algumas de buffer. Bibliotecas: `@tanstack/react-virtual` (mais flexível), `react-window` (mais simples). Funcionamento: baseado no scroll atual, calcula quais itens ficam visíveis e renderiza apenas eles — o resto é espaço vazio. Adicionalmente: paginação server-side (não carregue 10k itens de uma vez), lazy loading de colunas, sortable server-side.',
    tags: ['virtualizacao', 'tabela', 'react-virtual', 'react-window', 'performance'],
  },
  {
    id: 'rf-082',
    domain: 'react',
    type: 'open_text',
    difficulty: 2,
    targetLevel: ['pleno'],
    text: 'O que é debouncing e throttling? Quando usar cada um em React?',
    hints: ['Debounce: espera inatividade', 'Throttle: limita frequência', 'Lodash ou implementação custom'],
    explanation: 'Debounce: executa a função apenas após um período de inatividade (delay). Ideal para: search ao digitar (aguarda o usuário parar), validação de formulário. Throttle: limita a execução a no máximo uma vez por período de tempo. Ideal para: scroll handler, resize, mouse move — eventos que disparam com alta frequência onde você quer processamento regular mas não em cada evento. Em React: `useMemo(() => debounce(fn, 300), [])` ou biblioteca `use-debounce`/`lodash.debounce`. Limpe sempre no cleanup do useEffect.',
    tags: ['debounce', 'throttle', 'performance', 'eventos', 'optimizacao'],
  },
  {
    id: 'rf-083',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Quais são as causas mais comuns de memory leaks em React?',
    hints: ['Subscriptions sem cleanup', 'Event listeners', 'SetTimeout/setInterval', 'Closures com referências antigas'],
    explanation: 'Causas comuns: (1) Subscription sem cancelar: `socket.on("msg", handler)` sem `socket.off` no cleanup; (2) `setInterval`/`setTimeout` sem `clearInterval`/`clearTimeout` no cleanup; (3) Event listeners no window sem remoção; (4) Fetch sem cancelamento via AbortController — callback tenta atualizar state de componente desmontado; (5) Closures que retêm referências a objetos grandes. Prevenção: sempre retorne uma função de cleanup do `useEffect` que desfaz o efeito. Use ESLint plugin react-hooks/exhaustive-deps para detectar deps faltantes.',
    tags: ['memory-leak', 'cleanup', 'useEffect', 'subscriptions', 'listeners'],
  },
  {
    id: 'rf-084',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como otimizar páginas com muitas chamadas de API?',
    hints: ['Paralelismo', 'Caching', 'Suspense + React Query', 'Waterfall de requests'],
    explanation: 'Estratégias: (1) Evitar waterfall: não deixar requests em sequência quando não há dependência — use `Promise.all` para requests paralelos; (2) TanStack Query: cache automático, revalidação inteligente, deduplicação de requests (mesma query em múltiplos componentes = 1 request); (3) Prefetching: buscar dados antes do usuário navegar para a rota; (4) Suspense + lazy loading para carregamento progressivo; (5) Otimizar payload: GraphQL ou BFF para buscar apenas campos necessários; (6) SWR/stale-while-revalidate: mostrar dado em cache enquanto revalida.',
    tags: ['api', 'performance', 'cache', 'react-query', 'waterfall', 'paralelo'],
  },
  {
    id: 'rf-085',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que é hidratação (hydration) no React? O que pode dar errado?',
    hints: ['SSR HTML + React JS', 'Mismatch de conteúdo', 'useId para consistência'],
    explanation: 'Hidratação é o processo onde React "assume o controle" do HTML estático gerado pelo servidor — adiciona event listeners e reconcilia com o Virtual DOM sem re-renderizar o HTML. Erros comuns: (1) Hydration mismatch: o HTML do servidor difere do que o cliente renderizaria — causado por datas/horas (timezone diferente), Math.random(), conteúdo baseado no device, ou uso de `window` no render; (2) Solução: `suppressHydrationWarning`, `useId` para IDs únicos consistentes, `useLayoutEffect` apenas no cliente. No React 18+: erros de hidratação são recuperáveis em alguns casos.',
    tags: ['hidracao', 'ssr', 'mismatch', 'nextjs', 'useId'],
  },
  {
    id: 'rf-086',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'O que é bundle optimization no contexto React? Quais técnicas usar?',
    hints: ['Analisar bundle', 'Tree shaking', 'Code splitting', 'Lazy loading'],
    explanation: 'Técnicas: (1) Analisar com `webpack-bundle-analyzer` ou `vite-bundle-analyzer` — identifica o que está pesado; (2) Code splitting por rota: `React.lazy` + `Suspense`, ou `next/dynamic`; (3) Tree shaking: importações named em vez de default para libs grandes (ex: `import {format} from "date-fns"` não `import _ from "lodash"`); (4) Dynamic imports para features raramente usadas; (5) Externalizar deps comuns se múltiplos micro-frontends; (6) Substituir libs grandes por alternativas menores (ex: `dayjs` no lugar de `moment.js`).',
    tags: ['bundle', 'tree-shaking', 'code-splitting', 'webpack', 'optimizacao'],
  },
  // ── SISTEMA DE DESIGN FRONTEND ───────────────────────────────────────────────
  {
    id: 'rf-087',
    domain: 'react',
    type: 'open_text',
    difficulty: 4,
    targetLevel: ['senior', 'staff'],
    text: 'Como desenhar uma arquitetura frontend escalável para um projeto grande?',
    hints: ['Feature-based estrutura', 'Separação de camadas', 'Shared packages', 'Convenções'],
    explanation: 'Estrutura feature-based: `src/features/auth/`, `src/features/products/`, cada uma com seus componentes, hooks, services e tipos. Camadas: UI (componentes), Application (hooks, context), Infrastructure (services de API). Shared: `src/components/` (UI genérico), `src/hooks/` (hooks reutilizáveis), `src/utils/`. Convenções: naming, estrutura de arquivos, padrões de imports (index.ts para exports). Monorepo para apps múltiplos. ADRs para decisões de arquitetura. CI com lint, tests e bundle size checks.',
    tags: ['arquitetura', 'feature-based', 'escalabilidade', 'estrutura', 'monorepo'],
  },
  {
    id: 'rf-088',
    domain: 'react',
    type: 'open_text',
    difficulty: 4,
    targetLevel: ['senior', 'staff'],
    text: 'Como estruturar um projeto React grande? Monólito vs micro-frontend?',
    hints: ['Times e autonomia', 'Bundle duplication', 'Shared design system'],
    explanation: 'Monólito (Monorepo): único app, múltiplos times, shared components/types. Melhor para: times menores, consistência fácil, menos complexidade operacional. Micro-frontends: cada time tem seu app independente, deployado separadamente. Melhor para: times muito grandes (100+), domínios distintos que precisam de autonomia de deploy, diferentes stacks por time. Desafios de micro-frontends: bundle duplication, comunicação entre apps, design consistency. Comece com monólito bem estruturado — migre para micro-frontends apenas quando a dor for real.',
    tags: ['estrutura', 'monorepo', 'micro-frontends', 'times', 'escalabilidade'],
  },
  {
    id: 'rf-089',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como implementar componentes reutilizáveis de alta qualidade?',
    hints: ['API mínima e previsível', 'Composição sobre configuração', 'Acessibilidade', 'Documentação'],
    explanation: 'Princípios: (1) API mínima — exponha apenas o que o consumidor precisa; (2) Composição: use `children` e `slots` em vez de dezenas de props de configuração; (3) Polimorfismo: `as` prop para mudar a tag renderizada; (4) Ref forwarding para acesso ao DOM; (5) Acessibilidade: ARIA correto, foco gerenciado, teclas de atalho; (6) Sem estilos de layout embutidos (margem) — responsabilidade do consumidor; (7) Documentação com Storybook; (8) Testes unitários de comportamento.',
    tags: ['componentes', 'reutilizaveis', 'api', 'composicao', 'acessibilidade'],
  },
  {
    id: 'rf-090',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como implementar feature flags em React?',
    hints: ['Config remota', 'Context com flags', 'GrowthBook, LaunchDarkly', 'A/B testing'],
    explanation: 'Abordagem: (1) Config remota: plataforma como LaunchDarkly, GrowthBook ou Flag Smith — flags são controladas sem deploy; (2) Hook: `useFeatureFlag("nova-ui")` retorna boolean; (3) Componente: `<FeatureFlag name="nova-ui"><NovaUI/></FeatureFlag>`; (4) A/B testing: flag pode ter variantes para diferentes grupos de usuários; (5) Targeting: habilitar para usuário beta, empresa específica, % do tráfego; (6) Rollback instantâneo: desative flag sem deploy. Não hardcode feature flags em código — elas devem viver em config remota.',
    tags: ['feature-flags', 'a-b-testing', 'launchdarkly', 'growthbook', 'rollout'],
  },
  {
    id: 'rf-091',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como tratar autenticação de forma segura em React?',
    hints: ['httpOnly cookies', 'Não armazene tokens em localStorage', 'Refresh token rotation'],
    explanation: 'Práticas seguras: (1) Armazenar tokens em httpOnly cookies — não acessíveis por JavaScript, protegidos contra XSS; (2) NUNCA em localStorage — vulnerável a XSS; (3) CSRF protection com SameSite cookies e tokens CSRF; (4) Refresh token rotation — cada uso invalida o anterior; (5) No React: contexto de autenticação com estado `user` e funções `login`/`logout`; (6) Rotas protegidas que redirecionam se não autenticado; (7) Token expiration handling com interceptors no Axios/fetch; (8) Logout em todas as abas com BroadcastChannel.',
    tags: ['autenticacao', 'seguranca', 'tokens', 'cookies', 'csrf', 'jwt'],
  },
  {
    id: 'rf-092',
    domain: 'react',
    type: 'open_text',
    difficulty: 4,
    targetLevel: ['senior', 'staff'],
    text: 'Como você debugaria problemas de performance em produção em uma app React?',
    hints: ['Lighthouse', 'Web Vitals', 'Sentry Performance', 'React Profiler'],
    explanation: 'Processo: (1) Métricas: verificar Core Web Vitals via Sentry Performance, Datadog RUM, ou Google Search Console; (2) Identificar qual métrica está ruim (LCP/INP/CLS); (3) Lighthouse no DevTools para diagnóstico inicial; (4) Para INP lento: Performance tab do DevTools → Long Tasks → qual JS está bloqueando; (5) React Profiler (DevTools) → componentes com render longo; (6) Para LCP: verificar image loading, SSR, TTFB; (7) Para CLS: verificar dimensões de imagens/ads; (8) Session replay (Sentry, Hotjar) para reproduzir o problema.',
    tags: ['debugging', 'producao', 'performance', 'lighthouse', 'rum', 'profiler'],
  },
  {
    id: 'rf-093',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como lidar com retries de API no frontend?',
    hints: ['TanStack Query retry', 'Exponential backoff', 'Quais erros merecem retry'],
    explanation: 'Implementação: (1) TanStack Query: `useQuery({ ..., retry: 3, retryDelay: attemptIndex => Math.min(1000 * 2 ** attemptIndex, 30000) })`; (2) Exponential backoff manual: 1s, 2s, 4s, 8s (com jitter randômico para evitar thundering herd); (3) NÃO faça retry em erros 4xx (bad request, not found, unauthorized) — são erros do cliente que não vão resolver sozinhos; (4) Faça retry apenas para erros de rede e 5xx; (5) Circuit breaker: após muitas falhas, pare temporariamente; (6) Feedback visual ao usuário sobre tentativas em andamento.',
    tags: ['retry', 'api', 'exponential-backoff', 'circuit-breaker', 'react-query'],
  },
  {
    id: 'rf-094',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Quais são as estratégias de caching no frontend?',
    hints: ['HTTP Cache', 'Service Worker', 'In-memory (React Query)', 'Stale-while-revalidate'],
    explanation: 'Camadas: (1) HTTP Cache (Cache-Control): `max-age=3600` — browser não faz request se dado é recente; `stale-while-revalidate=60` — mostra dado em cache e revalida em background; `no-store` para dados sensíveis; (2) Service Worker (PWA): cache de assets e rotas offline; (3) In-memory (TanStack Query/SWR): `staleTime` — quanto tempo dado é fresco; `gcTime` — quanto tempo mantém em cache; (4) localStorage/sessionStorage: para dados que devem persistir entre sessões. Cache invalidation: o problema difícil — use keys baseadas no conteúdo (content hashing) para assets.',
    tags: ['cache', 'http-cache', 'service-worker', 'react-query', 'swr'],
  },
  {
    id: 'rf-095',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Implemente um custom hook `useDebounce`. Explique cada linha.',
    hints: ['setTimeout no useEffect', 'Cleanup do timeout', 'Retorna valor debounced'],
    explanation: `\`\`\`typescript
function useDebounce<T>(value: T, delay: number): T {
  const [debounced, setDebounced] = useState<T>(value);

  useEffect(() => {
    // Agenda atualização do valor debounced após delay
    const timer = setTimeout(() => setDebounced(value), delay);
    // Cleanup: cancela o timeout se value ou delay mudar antes do delay expirar
    return () => clearTimeout(timer);
  }, [value, delay]);

  return debounced;
}
// Uso: const debouncedSearch = useDebounce(inputValue, 300);
\`\`\`
Quando value muda, o useEffect cancela o timer anterior (cleanup) e cria um novo. Se value parar de mudar por 'delay' ms, o setDebounced é chamado e os componentes que usam debouncedSearch re-renderizam.`,
    tags: ['custom-hook', 'debounce', 'useEffect', 'useDebounce', 'live-coding'],
  },
  {
    id: 'rf-096',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Implemente um custom hook `useFetch` com loading, error e data.',
    hints: ['useState para data/loading/error', 'useEffect para fetch', 'AbortController para cancelamento'],
    explanation: `\`\`\`typescript
function useFetch<T>(url: string) {
  const [data, setData] = useState<T | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  useEffect(() => {
    const controller = new AbortController();
    setLoading(true);
    setError(null);

    fetch(url, { signal: controller.signal })
      .then(r => { if (!r.ok) throw new Error(r.statusText); return r.json(); })
      .then(setData)
      .catch(err => { if (err.name !== 'AbortError') setError(err); })
      .finally(() => setLoading(false));

    return () => controller.abort(); // Cancela na desmontagem ou mudança de url
  }, [url]);

  return { data, loading, error };
}
\`\`\``,
    tags: ['custom-hook', 'useFetch', 'AbortController', 'loading', 'live-coding'],
  },
  {
    id: 'rf-097',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como praticar debugging em React? Quais ferramentas e técnicas você usa?',
    hints: ['React DevTools', 'console.log estratégico', 'breakpoints', 'Error boundaries'],
    explanation: 'Ferramentas e técnicas: (1) React DevTools: inspecione props, state e contexto de qualquer componente; Profiler para identificar gargalos de performance; (2) Breakpoints no DevTools do browser — pause a execução em qualquer linha; (3) `console.log` estratégico — especialmente em useEffect para verificar quando dispara e com quais valores; (4) `debugger` statement no código; (5) Error Boundaries para capturar erros de renderização; (6) Network tab para verificar requests/responses; (7) Sentry em produção para capturar erros reais com contexto.',
    tags: ['debugging', 'react-devtools', 'breakpoints', 'console', 'ferramentas'],
  },
  {
    id: 'rf-098',
    domain: 'react',
    type: 'open_text',
    difficulty: 4,
    targetLevel: ['senior', 'staff'],
    text: 'Como explicar tradeoffs de arquitetura em uma entrevista? Dê um exemplo prático.',
    hints: ['Estruture como: opção A vs B, pros/contras, quando cada um', 'Seja específico com consequências'],
    explanation: 'Estrutura para comunicar tradeoffs: "Existem duas abordagens principais: A e B. A é melhor quando X porque Y. B é melhor quando Z porque W. Para este caso específico, eu escolheria A porque [razão concreta relacionada ao contexto do problema]." Exemplo: Context vs Redux para state global. Context: simples, zero deps, ótimo para dados estáticos. Redux: boilerplate (reduzido com RTK), DevTools, testabilidade. Escolha: para app pequena com poucos dados globais → Context. Para app complexa com muitas interações assíncronas → Redux/Zustand. Demonstre que você pensa em consequências, não em preferências.',
    tags: ['tradeoffs', 'comunicacao', 'entrevista', 'arquitetura', 'decisao'],
  },
  {
    id: 'rf-099',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Como praticar comunicação de soluções técnicas em entrevistas?',
    hints: ['Pense em voz alta', 'Confirme entendimento', 'Considere casos extremos', 'Mencione tradeoffs'],
    explanation: 'Técnicas para entrevistas técnicas: (1) Confirme o entendimento do problema antes de codificar: "Entendo que preciso de X, posso perguntar sobre Y?"; (2) Pense em voz alta — entrevistadores querem ver seu raciocínio, não apenas a solução; (3) Comece com solução ingênua, depois otimize — demonstra processo; (4) Mencione edge cases: "O que acontece se o array estiver vazio? E se o input for null?"; (5) Mencione tradeoffs: "Poderia usar A, que é mais simples, ou B, que tem melhor performance — dado que..."; (6) Pergunte sobre constraints: "Qual escala precisamos suportar?".',
    tags: ['comunicacao', 'entrevista', 'pensamento-em-voz-alta', 'tradeoffs', 'processo'],
  },
  {
    id: 'rf-100',
    domain: 'react',
    type: 'open_text',
    difficulty: 3,
    targetLevel: ['pleno', 'pleno-senior'],
    text: 'Quais fundamentos de JavaScript são mais cobrados em entrevistas React de nível Pleno/Sênior?',
    hints: ['Event loop', 'Closures', 'Protótipos', 'Async/await', 'This binding'],
    explanation: 'Fundamentos críticos: (1) Event loop e Call stack: por que setTimeout(fn, 0) não é imediato, Microtasks vs Macrotasks, Promise.then vs setTimeout; (2) Closures: como useCallback e useMemo funcionam, stale closures em useEffect, factory functions; (3) This binding: arrow functions vs function regulares, perda do this em callbacks; (4) Promises e async/await: encadeamento, tratamento de erro, Promise.all/race/allSettled; (5) Protótipos e herança: prototype chain, class vs prototype; (6) Imutabilidade: spread operator, structuredClone; (7) ES6+: destructuring, generators, WeakMap.',
    tags: ['javascript', 'fundamentos', 'event-loop', 'closures', 'async', 'entrevista'],
  },
]
